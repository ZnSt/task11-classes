const searchForm = document.querySelector('.search-form');
const divContainer = document.querySelector('.gallery');
const loadMore = document.querySelector('.load-more');

export { searchForm, divContainer, loadMore };
